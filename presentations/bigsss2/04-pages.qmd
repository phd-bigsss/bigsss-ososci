---
bibliography: "bib/phd-tesis.bib" 
---

# GitHub Pages {background-color="#aa0000"}

## Where we are

At this point you have:

. . .

✅ &nbsp; A reproducible project following the IPO protocol (`bigsss-repo/`)

. . .

✅ &nbsp; A local Git repository with a full commit history

. . .

✅ &nbsp; That repository live on GitHub at `github.com/phd-bigsss/bigsss-repo`

. . .

One step remains:

> Turning your Quarto HTML report into a **public website**  
> anyone can visit without installing R.

---

## What is GitHub Pages?

GitHub Pages is a **free static website hosting service** built into GitHub.

. . .

You point it at a folder or branch in your repo that contains HTML files.  
GitHub serves those files as a website at a public URL:

```
https://phd-bigsss.github.io/bigsss-repo
```

. . .

No server to configure. No hosting bill. No WordPress.  
The website updates every time you push new HTML to GitHub.

---

## Why it works perfectly with Quarto

When you run `quarto render` on your `.qmd` file, you get:

```
main_document.html    ← a complete, self-contained HTML file
```

. . .

GitHub Pages serves HTML files. Quarto produces HTML files.

. . .

```
quarto render → main_document.html → git push → GitHub Pages → public URL
```

. . .

The Quarto report you built in Part 1 is **already the right format**.  
We just need to tell GitHub where to find it.

---

## The URL structure

```
https://phd-bigsss.github.io/bigsss-repo
```

. . .

:::: {.columns}

::: {.column width="48%"}
**Examples**

```
phd-bigsss.github.io/bigsss-repo
julia.github.io/dissertation-ch2
research-lab.github.io/cohesion-study
```
:::

::: {.column width="48%"}
**What determines it**

- Your GitHub username (or organisation)
- The repository name you chose
- Nothing else to configure
:::

::::

. . .

::: {.callout-tip}
Your username becomes part of the URL — worth keeping it professional if you don't have one yet.
:::

---

## The approach

**Render locally → commit → push → GitHub Pages serves the HTML.**

. . .

```
bigsss-repo/
├── index.html           ← produced by quarto render
├── main_document.qmd    ← your source
└── ...
```

. . .

GitHub Pages is configured to serve files from the root of the `main` branch.  
No extra branches, no extra commands — just your normal Git workflow.

---

## One required YAML change

Add two options to your document's YAML header before publishing.

```yaml
---
title: "Study Report"
author: "Julio"
date: today
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
    theme: cosmo
    code-fold: true
    self-contained: true    # ← bundle CSS/JS into the file
    output-file: index.html # ← Pages looks for index.html at the root URL
execute:
  echo: false
  warning: false
---
```

. . .

`self-contained: true` — everything is bundled into one portable `.html` file.  
`output-file: index.html` — GitHub Pages serves `index.html` at the root URL by default.

---

## Configure GitHub Pages

Do this **once** in your repo settings on GitHub:

. . .

> **Repository → Settings → Pages**  
> Source: *Deploy from a branch*  
> Branch: `main` — Folder: `/ (root)` → **Save**

. . .

Your site will be live at:

```
https://phd-bigsss.github.io/bigsss-repo
```

GitHub takes about **1–2 minutes** to deploy after the first push.

---

## The workflow

From the **RStudio Terminal**, inside your project:

```bash
quarto render                          # 1. produces index.html
git add .                              # 2. stage everything
git commit -m "Render report"          # 3. commit
git push                               # 4. push → Pages updates automatically
```

. . .

No new commands to learn — just your normal Git workflow with a `quarto render` at the start.

---

## The update cycle

Every time you update your report:

. . .

1. Edit `main_document.qmd`
2. `quarto render` — re-generates `index.html`
3. `git add .`
4. `git commit -m "Update report"`
5. `git push` — GitHub Pages picks up the new HTML automatically

. . .

::: {.callout-note}
You are doing nothing new here — it is the same render → commit → push pattern  
you have been using all workshop. Pages just makes it publicly visible.
:::

---

## Linking everything together

Update your `README.md` to include the Pages link:

```markdown
# bigsss-repo

Reproducible research project following the IPO protocol.

## Live report

📄 [View the report](https://phd-bigsss.github.io/bigsss-repo)

## Repository structure

input/       Raw and processed data
processing/  Analysis scripts
output/      Tables and figures
```

. . .

Now anyone who visits your GitHub repo sees the live link immediately.

---

## The full open science workflow

```
Research question
      ↓
IPO protocol           (Part 1 — project structure, R scripts, Quarto)
      ↓
Version control        (Part 2 — Git: every decision is traceable)
      ↓
GitHub                 (Part 2 — remote backup, shareable, citable)
      ↓
GitHub Pages           (Part 2 — report as a public website)
      ↓
OSF / Zenodo           (future — persistent DOI, preregistration)
```

. . .

> From raw data to a public, reproducible, citable research report —  
> using only tools you already have open on your screen.

---

## What the site looks like

Your GitHub repo and published site work as a pair:

. . .

| `github.com/phd-bigsss/bigsss-repo` | `phd-bigsss.github.io/bigsss-repo` |
|---|---|
| Source code, scripts, data | Rendered HTML report |
| Commit history and diffs | What a reader sees |
| For collaborators and reviewers | For everyone else |

. . .

Both URLs are worth sharing — the repo for transparency,  
the Pages site for accessibility.

## Up next — hands-on

After the coffee break:

1. Add `self-contained: true` and `output-file: index.html` to your YAML
2. Run `quarto render` — confirm `index.html` appears in the folder
3. `git add .` → `git commit -m "Render report"` → `git push`
4. Configure GitHub Pages: Settings → Pages → main → / (root) → Save
5. Visit your live URL together
6. Make a small edit, re-render, commit, push — watch the update propagate
7. Add the Pages link to your README and push

. . .

> At the end of this block, paste your URL into the shared document.

---

## References

- Quarto — GitHub Pages publishing. <https://quarto.org/docs/publishing/github-pages.html>
- GitHub Pages documentation. <https://docs.github.com/en/pages>
- Bryan, J. (2024). *Happy Git and GitHub for the useR*. <https://happygitwithr.com>
